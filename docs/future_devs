- Ensure rogue lines / buses (i.e. those that are not connected to the main network) are removed or ignored from power flow to avoid errors
- It might be better to avoid manipulating self.xxx and instead pass each dataframe between methods then finally set the dataframe to self.xxx at the end of the method chain
- https://pandapower.readthedocs.io/en/latest/topology/examples.html
